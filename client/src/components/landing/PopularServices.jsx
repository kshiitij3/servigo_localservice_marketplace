const icons = { plumbing: "🔧", electrician: "⚡", electrical: "⚡", "ac-repair": "❄️", ac: "❄️", cleaning: "🧹", carpenter: "🪚", painting: "🎨", appliance: "🔌", default: "🛠️" };
const PopularServices = ({ categories, loading, onSelect }) => <section id="services" className="bg-gray-50 px-5 py-20 sm:py-24"><div className="mx-auto max-w-6xl"><h2 className="text-3xl font-extrabold sm:text-4xl">Popular Services</h2><p className="mt-3 text-gray-600">Start with a service category and find help nearby.</p>{loading ? <p className="mt-10 text-gray-500">Loading services...</p> : categories.length ? <div className="mt-10 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-6">{categories.slice(0, 6).map((category) => <button key={category._id || category.slug} onClick={() => onSelect(category.name)} className="rounded-xl border border-gray-100 bg-white p-5 text-left shadow-sm transition hover:-translate-y-1 hover:border-[#1a7a6e] hover:shadow-md"><span className="text-3xl">{icons[category.slug] || icons.default}</span><span className="mt-4 block font-bold">{category.name}</span></button>)}</div> : <p className="mt-10 rounded-lg bg-white p-5 text-gray-500">Services will appear here soon.</p>}</div></section>;
export default PopularServices;
